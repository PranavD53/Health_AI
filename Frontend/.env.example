# Backend API base URL used for REST, WebSocket, and uploaded file links.
# Set this when the frontend and backend run on different hosts (cloud deploy).
# For local dev with Vite proxy, you can leave this empty OR set:
# VITE_API_BASE_URL=http://127.0.0.1:8000
VITE_API_BASE_URL=
